const A = 100, B = 200;
var x, y, z;
begin
    x := 1;
    y := 2;
    z := 3;
    if (A + 101) > B then
    begin
        x := A + B;
        x := x / 10;
        if x > 100 then
            y := B * 4;
        if x < 100 then
            z := (A + B) * 70;
    end;
end.
