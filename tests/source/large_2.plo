const X = 100, Y = 200, Z = 300;
var a, b, c, d, e, f, g, h, i;
begin
    a := b + c;
    d := d + g;
    
    if a < b then
    begin
        f := g
    end;
    
    if a > b then
    begin
        a := b
    end;
    
    if a = b then
    begin
        b := h
    end;
    
    while a + b > c do
    begin
        a := a + 1;
    end;
end.
