/* Simple PL/0 Test Program */


/* This code should be the equivalent of this python function.

def factorial(n)
    if n < 2:
        return n
    else:
        return factorial(n - 1) * n
*/

begin
    const n = 6;
    var result, m;

    procedure factorial;
    begin
        if m < 2 then
            result := result * m;
        else
            begin
            m := m - 1;
            call factorial;
            result := 
        end
    end
    
    result := 0;
    m := 0;
    call factorial;
    write result;
end.
